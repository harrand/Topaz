shader(type = compute);
kernel(4, 1, 1);

resource(id = 0) buffer ColourData
{
	vec4 colours[4];
};

void main()
{
	uint id = in::local_id[0];
	float strength = length(colours[id]);
	float sign = strength > 1.0f ? 0.01f : 1.0002f;
	colours[id] = colours[id] * sign;
}
